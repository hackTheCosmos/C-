#include <iostream> // input output stream

using namespace std; // espace de nom

int main() //fonction principale (affiche du texte dans la console)
{
    // cout = "c" "out", endl = retour à la ligne
    cout << "Hello world!" << endl << "What's up ?" << endl; //affiche un message
    return 0; // termine le programme
}
